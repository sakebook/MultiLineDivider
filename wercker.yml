build:
  box:
      id: sakebook/docker-android-alpine
      username: $USERNAME
      password: $PASSWORD
      tag: latest
  steps:
    - script:
        name: unit test
        code: |
          ./gradlew clean :multilinedivider:testDebugUnitTest
